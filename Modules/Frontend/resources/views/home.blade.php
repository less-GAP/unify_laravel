<FrontendNewPatientForm
:title="{{ json_encode($data['title']) }}"
:logo="{{ json_encode($data['logo']) }}"
:states="{{ json_encode($data['states']) }}"
:insuranceCoverages="{{ json_encode($data['insuranceCoverages']) }}"
></FrontendNewPatientForm>